eureka:
   client:
      service-url:
         default-zone: http://localhost:8761/eureka/    
server:
   port: 8084
management:
   endpoints:
      web:
         exposure:
            include: hystrix.stream
